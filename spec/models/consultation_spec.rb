require 'rails_helper'

RSpec.describe Consultation, type: :model do
  pending "Should always be assigned to a user and a consultant"
  pending "c_time should never be empty"
  pending "status should never be empty"
  pending 'for status complete, payment amount should never be empty'
end
